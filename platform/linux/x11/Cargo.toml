[package]
name = "verge-platform-linux-x11"
edition.workspace = true
version.workspace = true
license-file.workspace = true

[dependencies]
verge-core = { path = "../../../core" }

[target.'cfg(target_os = "linux")'.dependencies]
x11rb = { version = "0.14", features = ["shape"] }
